export enum Endpoints {
	// auth
	AUTH_LOGIN = '/auth/login',
	AUTH_LOGOUT = '/auth/logout',

	// users
	USERS_ROOT = '/users',

	// food
	FOOD_ROOT = '/food',

	// categories
	CATEGORIES_ROOT = '/categories'
}
